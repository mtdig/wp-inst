[project]
name = "wp-inst"
version = "0.1.0"
description = "Ansible playbook to install WordPress with LAMP stack"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=13.4.0",
]
